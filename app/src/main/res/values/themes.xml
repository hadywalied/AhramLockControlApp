<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme" parent="Theme.Base">
        <!--customize the theme-->
    </style>

    <style name="Theme.Base" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/orange_700</item>
        <item name="colorPrimaryDark">@color/orange_800</item>
        <item name="colorAccent">@color/blue_600</item>
        <item name="colorSecondary">@color/blue_600</item>
        <item name="colorOnPrimary">@color/secondaryTextColor</item>
        <item name="colorOnSecondary">@color/primaryTextColor</item>
        <item name="colorOnSurface">@color/secondaryTextColor</item>
    </style>


    <style name="Theme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="Theme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="titleTextColor">@color/secondaryTextColor</item>
        <item name="popupTheme">@style/Theme.PopupOverlay</item>
    </style>

    <style name="Theme.chipOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="Theme.SplashScreen">
        <item name="colorPrimaryDark">@android:color/transparent</item>
    </style>

</resources>